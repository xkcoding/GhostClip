## 1. Android 基础资源

- [x] 1.1 下载 Space Grotesk 字体文件（SemiBold 600），放入 `res/font/space_grotesk_semibold.ttf`
- [x] 1.2 更新 `colors.xml`：统一设计稿色值（accent=#22C55E, accent-light=#F0FDF4, text-primary=#18181B, text-secondary=#71717A, text-muted=#A1A1AA, border=#E4E4E7, error-light=#FEF2F2, error-border=#FECACA, bg-muted=#F4F4F5）
- [x] 1.3 更新 `strings.xml`：补充设计稿中的文案（「请使用扫码功能连接 Mac 设备」「暂停同步」「解除配对」等）
- [x] 1.4 创建/更新 drawable 资源：已连接状态卡片背景（绿色填充+绿色边框+16px 圆角）、未连接状态卡片背景（红色填充+红色边框+16px 圆角）、同步列表卡片（白色+灰色边框+16px 圆角）、调试日志背景（#F4F4F5+12px 圆角）

## 2. Android 主界面对齐

- [x] 2.1 对齐 Header：appIcon（信封图标 32x21）+ appTitle（Space Grotesk 22sp -0.5 letterSpacing）+ settingsBtn（齿轮图标 22dp, 40x40 圆角 12）
- [x] 2.2 对齐已连接状态卡片：绿色背景+边框、绿色圆点+「已连接」标签、右侧解除配对按钮（红色浅背景+红色边框+圆角 10）、下方设备名+连接类型
- [x] 2.3 对齐未配对状态卡片：红色背景+边框、灰色圆点+「未连接」标签、右侧绿色「扫码配对」按钮、下方「请使用扫码功能连接 Mac 设备」提示
- [x] 2.4 对齐最近同步区域：标题「最近同步」（Space Grotesk 16sp 600）+ 右侧计数、圆角卡片列表+分割线+padding(14,16)
- [x] 2.5 实现同步列表项模板：方向图标（接收/发送）+ 内容预览（单行截断）+ 时间戳（相对时间）
- [x] 2.6 对齐调试日志区域：标题+复制/清除按钮、#F4F4F5 背景+12px 圆角+padding(12,14)、Courier New 10sp 日志文字

## 3. Android 设置页对齐

- [x] 3.1 对齐 Header：← 返回按钮 + 「设置」标题（Space Grotesk 24sp）
- [x] 3.2 实现「设备配对」分组：大写灰色标签（12sp 600 letterSpacing=1）+ 圆角卡片（配对状态行+分割线+MAC 标识行+分割线+解除配对行）
- [x] 3.3 实现「权限状态」分组：标签 + 卡片（电池优化行）
- [x] 3.4 实现「关于」分组：标签 + 卡片（版本号行+分割线+GitHub 行）
- [x] 3.5 统一卡片样式：圆角 16px + #E4E4E7 边框 1.5px + 内部行 padding(14,16) + 分割线 #E4E4E7 1px

## 4. Android 扫码页对齐

- [x] 4.1 对齐 Header：白色返回箭头 + 「扫码配对」标题（Space Grotesk 24sp 白色）
- [x] 4.2 对齐取景框：260x260 + 圆角 16 + 白色 3px 边框 + 扫描线（#22C55E 2px）
- [x] 4.3 对齐提示文案：「将 Mac 端显示的二维码放入框内」（白色 70% 透明度）+「请确保两台设备在同一 WiFi」（白色 40% 透明度）

## 5. Android 通知样式对齐

- [x] 5.1 对齐已配对通知：标题「GhostClip 运行中」+ 内容「已配对 {设备名} (局域网)」+ 操作按钮「暂停同步」「解除配对」
- [x] 5.2 对齐未配对通知：标题「GhostClip 运行中」+ 内容「未配对 · 等待扫码」+ 操作按钮「扫码配对」
- [x] 5.3 对齐收到剪贴板通知：标题「收到剪贴板内容」+ 内容「来自 {设备名} · {内容预览}」+ 操作按钮「复制」

## 6. Mac Dropdown 面板对齐

- [x] 6.1 对齐 Header：绿色圆点 10px + 「GhostClip」标题（Space Grotesk 14 600）+ 右侧状态文字（绿色「已连接」）
- [x] 6.2 对齐设备信息区：手机图标+设备名、WiFi 图标+连接类型+延迟
- [x] 6.3 对齐最近同步区：标签「最近同步」（11sp 600 letterSpacing=0.5）、方向箭头（↙ 绿色=接收, ↗ 黄色=发送）+ 内容 + 时间戳（相对时间）
- [x] 6.4 对齐操作区：发送剪贴板（send 图标+快捷键标注 ⌘⇧C）、设置（settings 图标）、配对（qr-code 图标）、解除配对（unlink 图标）、退出（power 图标 红色）
- [x] 6.5 对齐调试日志区：标签「调试日志」+ 清除按钮、#F4F4F5 圆角 4 背景、Courier New 10px 日志

## 7. Mac Settings 窗口对齐

- [x] 7.1 对齐快捷键区：标签「快捷键」+ 行（「发送剪贴板」+ 快捷键 badge）
- [x] 7.2 对齐设备配对区：标签「设备配对」+ 配对状态输入框 + MAC 标识输入框 + 「显示配对二维码」按钮行
- [x] 7.3 对齐通知区：标签「通知」+ toggle 开关行「接收同步时显示通知」+ 描述文案

## 8. Mac QR 弹窗对齐

- [x] 8.1 对齐独立 QR 弹窗：title bar「扫码配对」+ 关闭按钮、QR 码区域、说明文案（两行）、设备标识标签（monitor 图标 + 「MacBook Pro · gc-xxx」）
- [x] 8.2 对齐 Settings 内嵌 QR 弹窗：title bar 带返回按钮（← 返回）+ 标题、同样的 QR 内容布局
