<section class="section cta-section">
  <div class="container center">
    <h2 class="section-title fade-in">现在就试试</h2>
    <p class="cta-desc fade-in">30 秒完成配对，开始你的跨设备剪贴板体验</p>
    <a href="#download" class="cta-btn fade-in">回到顶部下载</a>
  </div>
</section>

<style>
  .cta-section {
    padding: 64px 0;
  }
  .cta-desc {
    font-size: 16px;
    color: #7A7A7A;
    margin-top: -32px;
    margin-bottom: 32px;
  }
  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--text);
    color: #fff;
    padding: 14px 32px;
    border-radius: 10px;
    font-family: var(--font-heading);
    font-size: 15px;
    font-weight: 500;
    transition: opacity 0.2s, transform 0.2s;
  }
  .cta-btn:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }
</style>
